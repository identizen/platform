# Changesets

Run `npm run changeset` to describe a change to a published package (`@identizen/protocol`, `@identizen/sdk`, `@identizen/react`, `identizen`). Versions are bumped with `npm run version-packages` and published with `npm run release`.
