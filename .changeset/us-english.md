---
'@identizen/react': patch
'@identizen/sdk': patch
'@identizen/index': patch
'@identizen/dashboard': patch
---

US English everywhere: error descriptions, OpenAPI descriptions, user-facing copy, README and doc comments now use US spellings (enroll, organization, license, canceled, behavior). Error codes, JSON fields and the SDK's `'cancelled'` session status are unchanged.
