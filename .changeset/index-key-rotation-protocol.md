---
'@identizen/protocol': minor
---

Index key rotation by cross-certification (PROTOCOL.md §3.1): `Rotation` and `SignedRotation` schemas, `signRotation`, `verifyRotation`, and `resolveIndexKey`, which walks a published chain of statements from a pinned key to the current one. New test vector `rotation.json`.
