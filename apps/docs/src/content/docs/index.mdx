---
title: Identizen
description: Login with your phone. Standard OIDC on the outside, a phone-held key on the inside.
template: splash
hero:
  title: Login with your phone.
  tagline: Your users' phones are the identity provider. You integrate a standard OIDC provider. Login is one tap plus Face ID.
  actions:
    - text: Quickstart
      link: /quickstart/
      icon: right-arrow
      variant: primary
    - text: Add MFA
      link: /add-mfa/
      variant: minimal
---

import DocCard from '../../components/DocCard.astro';
import LandingSteps from '../../components/LandingSteps.astro';

<section class="idz-section" aria-labelledby="start-here">
  <h2 id="start-here" class="idz-section-title">
    Start here
  </h2>
  <div class="idz-grid">
    <DocCard title="Quickstart" href="/quickstart/" icon="rocket" cta="Five minutes">
      A working "Continue with Identizen" button on a fresh Next.js app, with a fake phone so you
      can develop without a device.
    </DocCard>
    <DocCard title="Add MFA" href="/add-mfa/" icon="shield">
      Keep your password or SSO login. Bind the phone once, then push step-up and transaction
      approvals to it.
    </DocCard>
    <DocCard title="Framework guides" href="/guides/nextjs/" icon="layers" cta="Pick a stack">
      Next.js, Express, ASP.NET Core, Django, and plain HTML. Each is a normal OIDC integration with
      one extra claim to read.
    </DocCard>
    <DocCard title="Verification API" href="/reference/verification-api/" icon="terminal">
      Ask a phone to approve a transaction, poll or receive a signed webhook, and read the
      assertion.
    </DocCard>
    <DocCard title="SDK reference" href="/reference/sdk/" icon="book">
      The server SDK, the React components, and the CLI that scaffolds an app and runs a fake phone.
    </DocCard>
    <DocCard title="Self-hosting" href="/self-hosting/" icon="server">
      Run the index on your own Cloudflare account or as a Docker container, with Postgres behind
      it.
    </DocCard>
    <DocCard title="Demo: JT Merlin Bank" href="/examples/" icon="book" cta="See it live">
      A fictional bank at jtmerlin.com with passwordless login and wire approvals on the phone. Its
      developer pages show the real source.
    </DocCard>
  </div>
</section>

<section class="idz-section" aria-labelledby="how-it-works">
  <h2 id="how-it-works" class="idz-section-title">
    How a login works
  </h2>
  <LandingSteps />
</section>

{/* prettier-ignore */}
<section class="idz-section idz-split" aria-labelledby="copy-paste">
  <div>
    <h2 id="copy-paste" class="idz-section-title">
      Copy-paste to start
    </h2>
    <p>
      The CLI registers your app with an index, writes the OIDC config and route handlers for your
      framework, and runs a fake phone on localhost so the whole flow works before you have a device
      in hand.
    </p>
    <p>
      Nothing here is proprietary on the wire. The index is an OIDC provider; the phone holds an
      Ed25519 key and signs a challenge that names your site. The index stores public keys and never
      sees a password, because there is none.
    </p>
  </div>
  <div>

```bash title="Terminal"
npx identizen init
npx identizen dev
```

  </div>
</section>
